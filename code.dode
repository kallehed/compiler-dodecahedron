
fn main() {

    print_int(1);
    print_int(2);
    print_int(3);

    let a = 1;
    let b = 2;
    while a < 275 {
        print_int(a);
        let c = b;
        b = a + b;
        a = c;
    }

    #fibb(100,0,1);
    return 0;
}


fn fibb(n, start, start2) {
    let a = start;
    let b = start2;
    while a < n {
        print_int(a);
        let c = a + b;
        a = b;
        b = c;
    }
    return 0;
}
