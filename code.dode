fn main() {

    let tis_var = (1+(2))*3;

    other(0 * 1, 2 + 1, 3 + tis_var, 4, 5);
    
    let one = 1;
    let two = 2;

    let my_var = 1 + 3 * 2 + 9 + 8 * 2;
    
    let other_var = 1 + (2 * 3) + 4 * 5 + 6 * 7;

    other_var += 3;

    print_int(123 + one * two);

    while one < 10 {

        one += 2;
        one += other(a, b, c, d, e);
        print_int(one);
    }

}

fn other(a, b, c, d, e) {

}