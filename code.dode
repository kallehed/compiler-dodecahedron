fn main() {
    
    let one = 1;
    let two = 2;

    let my_var = 1 + 3 * 2 + 9 + 8 * 2;
    
    let tis_var = (1+(2))*3;

    let other_var = 1 + (2 * 3) + 4 * 5 + 6 * 7;
    other(0 * 1, 2 + 1, 3 + tis_var, 4, 5);

    printf("yolo: %d", 123);
}

fn other(a, b, c, d, e) {

}