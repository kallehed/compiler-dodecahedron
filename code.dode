fn main() {

    let tis_var = (1+(2))*3;

    other(0 * 1, 2 + 1, 3 + tis_var, 4, 5);
    
    let one = 1;
    let two = 2;

    let my_var = 1 + 3 * 2 + 9 + 8 * 2;
    
    let other_var = 1 + (2 * 3) + 4 * 5 + 6 * 7;
    

    printf("yolo: %d", 123 + one * two);

    while one < 5 {

        one += 1;
        printf("one is at: %d\n", one);
    }

}

fn other(a, b, c, d, e) {

}